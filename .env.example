# OpenAI API Configuration (Legacy - for other features)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify which OpenAI model to use (defaults to gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Google Gemini API Configuration (for AI-powered search)
# Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Upstash Redis (REQUIRED for data storage in production)
# The app accepts EITHER set of environment variables below.
# Get your credentials from https://console.upstash.com/

# Option 1: Use UPSTASH_REDIS_REST_URL/TOKEN (recommended)
UPSTASH_REDIS_REST_URL=your_upstash_redis_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# Option 2: Use KV_REST_API_URL/TOKEN (alternative/legacy naming)
# KV_REST_API_URL=your_upstash_redis_url_here
# KV_REST_API_TOKEN=your_upstash_redis_token_here

# Note: In development (NODE_ENV !== "production"), if neither set is configured,
# the app will automatically fall back to in-memory storage with a console warning.
# Data stored in-memory will be lost on server restart.

